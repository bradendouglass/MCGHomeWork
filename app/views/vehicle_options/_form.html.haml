= form_for @vehicle_option do |f|
  = f.hidden_field :vehicle_id, :value => params[:vehicle_id]
  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :description
    = f.text_field :description
  = f.submit 'Add Option', :class => "small button"
